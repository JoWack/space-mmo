# Copy to infra/.env and adjust. infra/.env is gitignored and must stay that way.
# These are local development credentials only — never reuse them anywhere real.

POSTGRES_DB=spacemmo
POSTGRES_USER=spacemmo
POSTGRES_PASSWORD=devonly
POSTGRES_PORT=5432
